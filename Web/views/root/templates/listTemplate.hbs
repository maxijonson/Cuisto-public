<table class="table table-bordered table-hover">
    <thead>
        <tr>
        
            <td colspan="5"> 
                {{!-- change the name for your list's name --}}
                <h3>Liste de...</h3>
            </td>
        </tr>
    </thead>
    <thead>
        <tr>
            {{!-- change the colspan for the number of title for your table --}}
            <td colspan="5"> 
                Liste de...
                {{!-- keep the label... (if not, it won't be inline --}}
                <label>
                    <select class="restaurantFrom">
                        {{#each infoSalles}}
                            <option>{{restaurant}}</option>
                        {{/each}}
                    </select>
                </label>
            </td>
        </tr>
    </thead>
    <tbody>
        <tr>
            {{!-- change the title for your columns' title --}}
            <th class="text-center">title1</th>
            <th class="text-center">title2</th>
            <th class="text-center">title3</th>
            <th class="text-center">title4</th>
            <th class="text-center">title5</th>
        </tr>
        {{!-- change the data name for your data name --}}
        {{#each data}}
            <tr>
                {{!-- change all cell's name for your datas' name --}}
                <td>{{cell1}}</td>
                <td>{{cell2}}</td>
                <td>{{cell3}}</td>
                <td>{{cell4}}</td>
                <td>{{cell5}}</td>
            </tr>
        {{/each}}
    </tbody>
</table>