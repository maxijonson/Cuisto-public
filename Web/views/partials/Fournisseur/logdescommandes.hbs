<!DOCTYPE html>
<html>

<head>
    <title>Archive des commandes</title>
    {{!-- add your CSS link --}}
    <link rel="stylesheet" type="text/css" href="/css/Fournisseurs/fournisseurs.css">

    {{!-- add your javascript link --}}
    <script src="/js/Commande/commande.js"></script>
</head>

<body>
    <table id="commandes" class="table table-bordered table-hover">
        <thead>
            <tr>
                <td colspan="3" style="overflow: hidden;">
                    <h3 style="float:left; margin-right: 10px;">Archive des commandes</h3>
                    <div class="input-group mb-3">
                        <select id="rechercheCommande" class="form-control">
                            <option value="-1">Tous les fournisseurs</option>
                            {{#each fournisseurs.fournisseurs}}
                            <option value="{{id}}">{{nom}}</option>
                            {{/each}}
                    </div>
                </td>
            </tr>
            <tr>
                <th class="text-center">Fournisseur</th>
                <th class="text-center">Date</th>
                <th class="text-center">Coût total</th>
            </tr>
        </thead>
        <tbody>
            {{#each commandes.commandes}}
            <tr id="{{id}}" class="selectable">
                <td id="nom">{{Fournisseur.nom}}</td>
                <td id="date">{{date}}</td>
                <td id="coutTotal">{{total}}$</td>
            </tr>
            {{/each}}
        </tbody>
        <tfoot>
            <tr>
                <td colspan="3">
                    <div style="width: 100%;">
                        <button id="btnVisionnerCommande" class="btn btn-primary selectAction" disabled>Voir la commande</button>
                    </div>
                </td>
            </tr>
        </tfoot>
    </table>
</body>